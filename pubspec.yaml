name: malaria_shield
description: "A new Flutter project."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # google_fonts: ^2.1.0
  firebase_core: ^2.1.0
  firebase_auth: ^4.1.2
  cloud_firestore: ^4.0.0
  firebase_database: ^10.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/background_image.jpg # Adjust the path according to your project structure
